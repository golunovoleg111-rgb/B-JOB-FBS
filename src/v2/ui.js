const esc = v => String(v ?? '').replace(/[&<>"']/g, c => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[c]));
const labels = { dashboard:'Главная', nomenclature:'Номенклатура', warehouse:'Склад FBS', inventory:'Учет склада', assembly:'Сборка', revisions:'Ревизия', transfers:'Перемещения', users:'Пользователи', settings:'Настройки', integrations:'Интеграции' };
const rights = { admin:['*'], manager:['dashboard','nomenclature','warehouse','inventory','assembly','revisions','transfers'], worker:['dashboard','warehouse','assembly'] };
export const can = (role, page) => rights[role]?.includes('*') || rights[role]?.includes(page);
export function renderLogin() { return `<main class="auth"><section class="login-card"><div class="logo">B-JOB <span>FBS</span></div><h1>Вход в систему</h1><p>Операционная платформа FBS</p><form id="login-form"><label>Логин<input name="login" autocomplete="username" required autofocus></label><label>Пароль<input name="password" type="password" autocomplete="current-password" required></label><button class="primary">Войти</button><div id="login-error" class="error"></div></form></section></main>`; }
export function renderShell(user) { const nav = Object.keys(labels).filter(p => can(user.role,p)); return `<div class="app"><aside><div class="logo">B-JOB <span>FBS</span></div><div class="user-card"><b>${esc(user.login)}</b><small>${user.role==='admin'?'Администратор':esc(user.role)}</small></div><nav>${nav.map(p=>`<button data-page="${p}">${labels[p]}</button>`).join('')}</nav><button id="logout" class="logout">Выйти</button></aside><main class="content"><header><div><span class="eyebrow">B-JOB FBS · v2</span><h1>Рабочая система</h1></div><span class="status">● Система активна</span></header><section id="page"><div class="hero"><span class="eyebrow">Новая архитектура</span><h2>Фундамент готов</h2><p>Следующий этап подключит рабочие операции.</p></div></section></main></div>`; }
