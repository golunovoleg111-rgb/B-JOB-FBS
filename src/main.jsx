import React,{useState}from'react';import{createRoot}from'react-dom/client';import'./styles.css';
const DEFAULT_ADMIN={id:'admin',login:'Admin1',password:'Admin123',role:'admin'};
function App(){const[login,setLogin]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const submit=e=>{e.preventDefault();if(login===DEFAULT_ADMIN.login&&password===DEFAULT_ADMIN.password)setError('');else setError('Неверный логин или пароль')};return <div className="page"><div className="card"><div className="logo">B-JOB <span>FBS</span></div><div className="eyebrow">FBS MANAGEMENT SYSTEM</div><h1>Вход в систему</h1><p>Авторизация администратора</p><form onSubmit={submit}><label>Логин<input value={login}onChange={e=>setLogin(e.target.value)}autoComplete="username"/></label><label>Пароль<input type="password" value={password}onChange={e=>setPassword(e.target.value)}autoComplete="current-password"/></label><button type="submit">Войти</button>{error&&<div className="error">{error}</div>}</form></div></div>};createRoot(document.getElementById('app')).render(<App/>);